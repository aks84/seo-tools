import 'package:flutter/material.dart';

class Sem extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(
        title: Text("SEM Tools"),
      ),
      body: Text('Explore all the SEM tools..'),
    );
  }
}
