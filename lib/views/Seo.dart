import 'package:flutter/material.dart';

import 'package:seo_tools/models/Seo.dart';

class Seo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(
        title: Text("SEO Tools"),
      ),
      body: Text('Explore all SEO the tools..'),
    );
  }
}
