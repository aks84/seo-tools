import 'package:flutter/material.dart';

class Blogging extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(
        title: Text("Blogging Tools"),
      ),
      body: Text('Explore all the Blogging tools..'),
    );
  }
}
