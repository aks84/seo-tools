import 'package:flutter/material.dart';

import 'package:seo_tools/models/Spy.dart';

class Spy extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(
        title: Text("Spy Tools"),
      ),
      body: Text('Explore all the Spy tools..'),
    );
  }
}
