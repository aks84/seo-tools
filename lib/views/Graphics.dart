import 'package:flutter/material.dart';

class Graphics extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(
        title: Text("Graphics Tools"),
      ),
      body: Text('Explore all the Graphics tools..'),
    );
  }
}
